<resources>
    <!-- Base application theme. -->
    <style name="Theme.AnimeWatch" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/gray_600</item>
        <item name="android:colorBackground">@color/dark</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ThemeOverlay.AnimeWatch.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/dark</item>
        <item name="fullscreenTextColor">@color/white</item>
    </style>
</resources>